return {
    -- Optional callbacks:
    -- OnSpawn = function(ent, cfg) end,
    -- OnThink = function(ent, cfg, stateName, target) end,
    -- OnStateChanged = function(ent, newState, cfg) end,
    -- SelectTarget = function(ent, defaultTarget) return defaultTarget end,
    -- SelectTeleportPos = function(ent, defaultPos) return defaultPos end,
}
